import Transition from './useTransition';
import Optimistic from './useOptimistic';

const Hooks = () => {
  return (
    <div>
      <Optimistic />
      <Transition />
    </div>
  );
};

export default Hooks;
